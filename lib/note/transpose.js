'use strict'

/**
 * Transpose a note by an (diatonic) interval
 *
 * An alias for `interval/transpose`
 *
 * @example
 * transpose('P5', 'D') // => 'A4'
 ^
 * @see interval/transpose
 */
module.exports = require('../interval/transpose')
