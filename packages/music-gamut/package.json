{
  "name": "music-gamut",
  "private": true,
  "main": "lib/index.js",
  "scripts": {
    "build": "cp README.md lib/ && npm test && npm run coverage && npm run docs && npm run dist",
    "test": "mocha",
    "docs": "documentation -f md > API.md",
    "dist": "browserify lib/index.js | uglifyjs > dist/music-gamut.min.js&& ls -hall dist/",
    "coverage": "istanbul cover _mocha"
  },
  "author": "danigb",
  "license": "MIT",
  "dependencies": {
    "array-notation": "^1.0.0",
    "note-interval": "file:../note-interval",
    "note-transpose": "file:../note-transpose"
  }
}
