{
  "name": "tonal-array",
  "version": "0.50.0",
  "description": "Conversion between array numbers and note names",
  "keywords": [
    "note",
    "array",
    "numbers",
    "tonal"
  ],
  "scripts": {
    "pretest": "rm -rf build && mkdir build && rollup -f umd -n array -o build/tonal-array.js -- index.js",
    "test": "tape 'test/*.js'"
  },
  "main": "build/tonal-array.js",
  "jsnext:main": "index",
  "author": "danigb",
  "license": "MIT",
  "devDependencies": {
    "rollup": "0.25",
    "tape": "4"
  },
  "dependencies": {
    "tonal-notation": "file:../notation",
    "tonal-pitch": "file:../pitch",
    "tonal-transpose": "file:../transpose"
  }
}
