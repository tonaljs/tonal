{
  "name": "tonal-transpose",
  "version": "0.50.2",
  "description": "Music note transposition",
  "keywords": ["note", "transpose", "numbers", "tonal"],
  "scripts": {
    "pretest": "rm -rf build && mkdir build && rollup -f umd -n transpose -o build/tonal-transpose.js -- index.js",
    "test": "tape 'test/*.js'"
  },
  "main": "build/tonal-transpose.js",
  "jsnext:main": "index",
  "author": "danigb",
  "license": "MIT",
  "devDependencies": {
    "rollup": "0.25",
    "tape": "4"
  },
  "dependencies": {
    "tonal-pitch": "file:../pitch"
  }
}
