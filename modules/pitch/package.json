{
  "name": "tonal-pitch",
  "version": "0.50.0",
  "description": "Music pitch utilities",
  "keywords": [
    "pitch",
    "tonal"
  ],
  "scripts": {
    "pretest": "rm -rf build && mkdir build && rollup -f umd -n pitch -o build/tonal-pitch.js -- index.js",
    "test": "tape 'test/*.js'"
  },
  "main": "build/tonal-pitch.js",
  "jsnext:main": "index",
  "author": "danigb",
  "license": "MIT",
  "devDependencies": {
    "rollup": "0.25",
    "tape": "4"
  },
  "dependencies": {
    "interval-notation": "^0.1.0",
    "note-parser": "^1.0.0",
    "tonal-encoding": "^0.50.0",
    "tonal-notation": "^0.50.0"
  }
}
